/vendor
/.idea
/.vscode
/.vagrant
.phpunit.result.cache
.env
builds/
storage/
storage/wback.toml
.box_dump/

# session notes and audit state - local to this clone, never published
CLAUDE.local.md

# Placeholder branding for the Slack webhook's icon, set in Slack's own UI - so
# nothing here reads these and no public URL is needed. Ignored rather than
# committed because this repo is public and git history is permanent: a
# placeholder committed now stays fetchable at that commit forever. Backed up
# regardless, since dev-backup mirrors ~/cli whether or not git tracks a file.
#
# Delete this line when there is a logo worth publishing - assets/ is where it
# should live, so that is the only change needed.
/assets/
